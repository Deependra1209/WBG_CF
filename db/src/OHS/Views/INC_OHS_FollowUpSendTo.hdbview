VIEW INC_OHS_FollowUpSendTo AS
SELECT 
    FST.FSTID, 
    FST.FUPID, 
    FST.EMPID,
    EMP.EMPNM AS FSTNM
FROM 
    "INC_T_FOLST" FST
LEFT JOIN "INC_M_EMPDT" EMP ON EMP.EMPID=FST.EMPID
WHERE FST.ISDEL='0';