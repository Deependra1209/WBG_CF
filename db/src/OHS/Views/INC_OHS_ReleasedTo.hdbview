VIEW INC_OHS_ReleasedTo AS
SELECT 
    RLS.RSLID,
    RLS.ENVID,
    RLS.INCID,
    RLS.RLTOI,
    MST.UNQID RLTOI_TXT
FROM 
    "INC_T_RLSTO" RLS 
LEFT JOIN "INC_M_MSTDT" MST ON MST.UNQID = RLS.RLTOI
WHERE 
    RLS.ISDEL = '0';